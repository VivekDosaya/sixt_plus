const Header = ({ title }) => {
  return (
    <h1 className="tracking-wider text-brandBlack text-3xl text-white  md:text-8xl text-center font-bold z-10   w-full ">
      {title}
    </h1>
  );
};

export default Header;
